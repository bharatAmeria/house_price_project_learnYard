{{- if and .Values.serviceAccount (hasKey .Values.serviceAccount "create") (eq .Values.serviceAccount.create true) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "flask-chart.serviceAccountName" . }}
{{- end }}
